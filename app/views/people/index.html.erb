<h1>People</h1>
<div id="table">
  <%= render :partial => 'table' %>
</div>
<div id="toolbar">
  <% if !logged_in? %>
  <%= link_to 'Sign Up', signup_path %>
  <% end %>
  <div id="pagination"><%= render :partial => 'pagination' %></div>
</div>
